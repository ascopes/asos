[package]
name = "asos"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# Pure rust BIOS bootloader.
bootloader = "0.10.2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
